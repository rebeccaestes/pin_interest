<h2> <%= @board.title %></h2>
<h3><%= link_to 'Edit this', edit_board_path(@board)%></h3>

<h3><%= link_to 'Add a new pin', new_board_pin_path(@board) %></h3>

<% @pins.each do |pin| %>
	<%= link_to board_pin_path(@board, pin) do %>
    	<%= image_tag pin.img_url, class: "pin_gallery" %><% end %>
	<%= pin.title %>
<% end %>
