<h2> <%= @pin.title %></h2>
<h3> a pin by <%= @board.user.email %></h3>
<% if @board.user == current_user %>
<h3><%= link_to 'Edit this', edit_board_pin_path(@board, @pin)%></h3>
<% end %>

<%= image_tag @pin.img_url, class: "pin_photo" %>
